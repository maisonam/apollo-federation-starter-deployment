apiVersion: v1
kind: Service
metadata:
  name: "wallet-api-service"
  namespace: "apollo-federation-dev"
spec:
  selector:
    app: "wallet-api"
  ports:
   -  protocol: TCP
      port: 80
      targetPort: 5002

